.left-contents
  .left-header
    .left-header__box
      %p.left-haeder__box__user-name
        = current_user.name
      %ul.left-haeder__box__menu
        =link_to new_group_path, class: "left-header__box__menu_new-group" do
          = fa_icon 'edit'
        =link_to edit_user_path(current_user) , class: "left-header__box__menu_new-group" do
          = fa_icon 'cog'
  .groups
    .group
      - current_user.groups.each do |group|
        = link_to group_messages_path(group) do
          %p.group__group-name
            = group.name
          %p.group__group-message
            まだメッセージはありません